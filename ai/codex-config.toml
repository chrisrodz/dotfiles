# Codex CLI Configuration
# Symlinked to ~/.codex/config.toml by bootstrap.sh

model = "gpt-5.2-codex"
model_reasoning_effort = "high"

# Project trust levels are machine-specific and NOT tracked here.
# Codex manages them automatically in ~/.codex/config.toml
